# Server
NODE_ENV=development
PORT=3001
SESSION_SECRET=change_me

# Database (local MySQL)
DB_NAME=hackathought
DB_USER=root
DB_PW=password
DB_HOST=localhost

# CORS (client origin)
CORS_ORIGIN=http://localhost:5173

# Optional (production / Heroku)
# JAWSDB_URL=
