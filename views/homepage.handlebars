{{#each blogs as |blog| }}
<div class="row mb-4 project">
  <div class="col-md-5">
    <p>Created by ID:{{id}}, NAME: {{title}} on {{createdAt}}</p>
  </div>
  <div class="col-md-7">
    <p>
      {{content}}
    </p>
  </div>
</div>
{{/each}}
